{
  "name": "fellowsRdBackend",
  "version": "1.0.0",
  "main": "handler.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "babel src --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "yarn build && sls deploy --aws-profile serverless_personal"
  },
  "dependencies": {
    "aws-sdk": "^2.213.1",
    "babel-cli": "^6.26.0",
    "bulma": "^0.7.1",
    "date-fns": "^1.29.0",
    "parcel-bundler": "^1.7.1",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0"
  }
}
