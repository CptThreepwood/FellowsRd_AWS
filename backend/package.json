{
  "name": "fellowsRdBackend",
  "version": "1.0.0",
  "main": "handler.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "babel src --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "yarn build && sls deploy --aws-profile personal"
  },
  "dependencies": {
    "aws-sdk": "^2.213.1",
    "uuid": "^3.2.1"
  },
  "devDependencies": {}
}
